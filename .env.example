# x402guard API Configuration
# Copy this file to .env.local and update values as needed

# Server-side API URL (used by API proxy route in /api/audit/[tier])
# Options:
#   - http://localhost:3001  (local API server)
#   - http://x402guard.xyz   (production)
API_URL=http://x402guard.xyz

# Client-side API URL (exposed to browser)
# Currently not used since we proxy through /api/audit/[tier]
NEXT_PUBLIC_API_URL=http://x402guard.xyz
